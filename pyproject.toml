[project]
name = "duckdunk"
version = "0.10.0"
description = "Powerful web searching using DuckDuckGo."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pillow", "beautifulsoup4", "requests", "w3lib"]
